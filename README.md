This repo is dedicated for testing rest api creation using java programming language

### Docker image 
````
docker container run --name {container name} -p {on witch port to run it}:8002 {image name}
```` 